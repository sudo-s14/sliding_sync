name: sliding_sync
description: Minimal Sliding Sync (MSC4186) reference implementation in Dart.
version: 0.1.0

environment:
  sdk: ^3.0.0
dev_dependencies:
  test: ^1.29.0
dependencies:
  http: ^1.6.0
